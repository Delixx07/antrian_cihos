APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:nuw7MkkQFtyEnAYIMU0HX0+MfUhDY3M2kaTlWuqOM0U=
APP_DEBUG=true
APP_URL=http://172.19.4.232/antrian/

# Basis URL untuk aset (gambar, audio TTS). Ditetapkan tetap agar aset
# tidak 404 saat halaman dibuka lewat URL pendek /display/...
ASSET_URL=/antrian

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=antrian_cihos
DB_USERNAME=root
DB_PASSWORD=

# Koneksi READ-ONLY ke DB MASTER bersama (data general: dokter/klinik/jadwal/prefix).
# Ditulis oleh aplikasi Appointment; antrian hanya membaca.
MASTER_DB_HOST=127.0.0.1
MASTER_DB_PORT=3306
MASTER_DB_DATABASE=cihos_master
MASTER_DB_USERNAME=root
MASTER_DB_PASSWORD=

# Koneksi READ-ONLY ke database aplikasi Appointment (data pasien/antrian bila perlu).
APPT_DB_HOST=127.0.0.1
APPT_DB_PORT=3306
APPT_DB_DATABASE=appointment_pasien_cihos
APPT_DB_USERNAME=root
APPT_DB_PASSWORD=

# Direktori user RS (login) — dbuser.user_detail, password SHA1.
DBUSER_HOST=127.0.0.1
DBUSER_PORT=3306
DBUSER_DATABASE=dbuser
DBUSER_USERNAME=root
DBUSER_PASSWORD=

# Aplikasi Appointment — sumber data registrasi/antrian (API).
# Base URL TANPA /public (rewrite XAMPP menangani ke public/index.php).
APPOINTMENT_API_URL=http://172.19.4.232/appointment
APPOINTMENT_API_KEY=20dc1213ca7aee65f01090769eede289
APPOINTMENT_API_TIMEOUT=5

SESSION_DRIVER=database
SESSION_LIFETIME=10080

# Suara: satu PC pusat (buka /display/speaker) yang mengumumkan untuk
# SELURUH rumah sakit. Layar TV lain diam.
TTS_SOUND_MODE=central

# ── Suara pengumuman (Edge TTS) ──
# Voice Indonesia yang tersedia: id-ID-GadisNeural (wanita), id-ID-ArdiNeural (pria).
# RATE  : besar = lebih cepat.  PITCH: negatif = nada lebih rendah/berat.
TTS_EDGE_VOICE="id-ID-GadisNeural"
TTS_EDGE_RATE="+8%"
TTS_EDGE_PITCH="-12Hz"

SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
# Nama cookie unik agar tak bentrok dgn aplikasi appointment di server sama.
SESSION_COOKIE=antrian_session

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# SQL Server MEDINFRAS (ODBC) — untuk baca vServiceUnitRoom (zona/room klinik).
SQLSRV_HOST=10.10.110.3
SQLSRV_PORT=1433
SQLSRV_DATABASE=MS_CHSBY_TEST
SQLSRV_USERNAME=itrs2
SQLSRV_PASSWORD=itrsdevCIHOS2
SQLSRV_ENCRYPT=no
SQLSRV_TRUST_SERVER_CERTIFICATE=true
